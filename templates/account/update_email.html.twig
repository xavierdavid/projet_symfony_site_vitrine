{% extends "base.html.twig" %}

{% block title %}Modification de l'adresse email{% endblock %}

{% block body %}
<main class="container">
  {% for hero in hero %}
    <section class="section-authentification-form">
      <div class="authentification-container">
        <div class="authentification-text-box">
          <header>
            <h1 class="heading-primary">Modifier mon adresse email</h1>
          </header>
          <p class="authentification-text">
              Saisissez la nouvelle adresse email que vous souhaitez associer à votre compte. Nous vous enverrons un lien de vérification à cette adresse pour l'activer. 
          </p>
          {# Formulaire de demande de modification de l'email #}
          <section>
            <p class="subheading">Adresse email actuelle :</p>
            <p class="authentification-text">{{user.email}}</p>
            {{form_start(formView, {'attr' : {'class': 'authentification-form'}})}}
              <div class="form-row">
                {{form_row(formView.new_email)}}
              </div>
              <br>
              {# Bouton de soumission du formulaire #}
              <button class="btn btn--form" type="submit">Envoyer</button>
            {{form_end(formView)}}
          </section>
          {# Lien vers la route de la page d'accueil du compte de l'utilisateur #}
          <section>
            <ul class="authentification-nav-list">
              <li class="authentification-list">
                <ion-icon class="icon icon-secondary" name="play-back"></ion-icon>
                <span>
                    <a href="{{path('app_account_home')}}">Retour vers mon compte</a>
                </span>
              </li>
            </ul>
          </section>
        </div>
        <div class="contact-cta-img-box" style="background-image:linear-gradient(to bottom, var(--gradient-main-tint), var(--gradient-main-lighter)), url({{asset('uploads/'~hero.masterImage)}})" role="img" aria-label="{{hero.masterImageDescription}}"></div>
      </div>
    </section>
  {% endfor %}
</main>
{% endblock %}