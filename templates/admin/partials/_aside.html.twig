{# Aside #}
<aside>
  <nav>
    <section>
      {% if is_granted('ROLE_AUTHOR')%}
        <div>
          <p><a href="{{path('app_admin_home')}}"><i class="fas fa-home"></i> Accueil</a></p>
        </div>
      {% endif %}
      {% if is_granted('ROLE_ADMIN')%}
        <div>
          <p><a href="{{path('app_admin_organization_index')}}"><i class="fas fa-building"></i> Organisation</a></p>
        </div>
        <div>
          <p><a href="{{path('app_admin_product_index')}}"><i class="fas fa-sitemap"></i> Services</a></p>
        </div>
      {% endif %}
      {% if is_granted('ROLE_AUTHOR')%}
        <div>
          <p><a href="{{path('app_admin_article_index')}}"><i class="fas fa-newspaper"></i> Articles</a></p>
        </div>
        {% endif %}
      {% if is_granted('ROLE_ADMIN')%}
        <div>
          <p><a href="{{path('app_admin_category_index')}}"><i class="fas fa-tasks"></i> Catégories</a></p>
        </div>
        <div>
          <p><a href="{{path('app_admin_administrator_index')}}"><i class="fas fa-user-tie"></i> Dirigeants</a></p>
        </div>
        <div>
          <p><a href="{{path('app_admin_collaborator_index')}}"><i class="fas fa-users"></i> Collaborateurs</a></p>
        </div>
        <div>
          <p><a href="{{path('app_admin_partner_index')}}"><i class="fas fa-handshake"></i> Partenaires</a></p>
        </div>
        <div>
          <p><a href="{{path('app_admin_contact_index')}}"><i class="fas fa-id-card-alt"></i> Messages</a></p>
        </div>
        <div>
          <p><a href="{{path('app_admin_document_index')}}"><i class="fas fa-file"></i> Documents</a></p>
        </div>
        <div>
        <div>
          <p><a href="{{path('app_admin_image_index')}}"><i class="fas fa-images"></i> Médias</a></p>
        </div>
          <p><a href="#"><i class="fas fa-desktop"></i> Carrousel</a></p>
        </div>
        <div>
          <p><a href="{{path('app_admin_metatag_index')}}"><i class="fas fa-map"></i> Metatags</a></p>
        </div>
        <div>
          <p><a href="{{path('app_admin_user_index')}}"><i class="fas fa-user"></i> Utilisateurs</a></p>
        </div>
      {% endif %}
    <section>
    <br>
  </nav>
</aside>
